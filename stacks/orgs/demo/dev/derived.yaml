# Test derived component overwriting 
components:
  terraform:
    derived/default:
      metadata:
        component: derived
        type: abstract
      settings:
        level1:
          example: default-value
      vars:
        namespace: demo
        tenant: core
        environment: ue1
        stage: dev
        foo: bar

    derived:
      metadata:
        component: derived
        inherits:
          - derived/default
      settings:
        level1:
          example: result
